# React 19 + the surrounding ecosystem are still publishing peer ranges that lag
# behind the React 19 release. legacy-peer-deps keeps `npm install` deterministic
# and conflict-free during this transition window.
legacy-peer-deps=true
